# Normalize line endings: all text files use LF in the repository
# (with core.autocrlf=false, LF is also checked out on Windows)
* text=auto
*.py text eol=lf
*.md text eol=lf
*.txt text eol=lf
*.gitignore text eol=lf
LICENSE text eol=lf
